doctype html
html(lang='en')
    head
        meta(charset='utf-8')
        title #{title}
    body
        h1 #{title} (using Pug)
        if emailsVisible
            h3 Emails:
            ul
                each email in emails
                    li=email
        else
            h3 Emails not found
        p Phone: #{phone}
